#include "main.h"

/**
 * mul - Computes the product of two integer inputs.
 * @x: First input.
 * @y: Second input.
 *
 * Return: Product of the two inputs.
 */
int mul(int x, int y)
{
	return (x * y);
}
