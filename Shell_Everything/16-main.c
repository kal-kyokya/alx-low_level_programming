#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	_setenv("DJ", "The_Dem", 0);
	_setenv("El", "Capro", 0);
	_setenv("El_pipe", "De_Oro", 0);

	return (0);
}
