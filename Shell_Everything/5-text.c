#include "Dj_dem"

/**
 * main - Entry point to the program.
 *
 * Return: 0 if program runs successfully.
 */
int main()
{
	char *ptr;
	// is a pointer (*ptr) to ONE or MANY variable(s) of type char.
	// [char] [*ptr]

	char **ptr;
	// is a pointer (*ptr) to ONE or MANY pointer(s) to ONE or MANY variable(s) of type char.
	// [char] {*[*ptr]}

	return (0);
}
