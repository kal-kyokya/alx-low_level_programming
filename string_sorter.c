#include <stdio.h>

/**
 * str_sorter - Arranges a set of string alphabetically.
 * @str: Pointer to a 2D array.
 * @a: Number strings in array.
 *
 * Return: A pointer to a 2D array.
 */
void *str_sorter(char **str, int a)
{
	int len1, len2, i, b;

	len1 = strlen(str[a]);
	len2 = strlen(str[b]);
	while (len1 !< 0 || len2 !< 0)
	{
		if (str[a][i] < str[b][i])
		  
		len1--;
		len2--;
	}
}
